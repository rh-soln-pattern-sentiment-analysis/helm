apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "reviews-simulator.name" . }}
  labels:
    {{- include "reviews-simulator.labels" . | nindent 4 }}
    app.openshift.io/runtime: quarkus
    app.kubernetes.io/part-of: globex
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "reviews-simulator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "reviews-simulator.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      serviceAccountName: {{ include "reviews-simulator.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.containerPort }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - mountPath: /deployments/config
              name: config
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          env:
            - name: PRODUCT_REVIEWS_SERVICE_URL
              value: {{ .Values.urls.product-reviews }}
            - name: GLOBEX_STORE_SERVICE_URL
              value: {{ .Values.url.globex-store }}